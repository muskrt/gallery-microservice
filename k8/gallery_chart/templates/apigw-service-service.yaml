apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f ./docker-compose.yaml -o gallery_chart/templates/
    kompose.image-pull-secret: regcred
    kompose.version: 1.32.0 (765fde254)
  labels:
    io.kompose.service: apigw-service
  name: apigw-service
spec:
  ports:
    - name: "30200"
      port: 30200
      targetPort: 30200
  selector:
    io.kompose.service: apigw-service
